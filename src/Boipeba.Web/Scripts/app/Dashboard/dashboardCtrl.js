(function () {
    "use strict";

    dashboardCtrl.$inject = ["$scope", "$http", "$window", "toastr"];

    function dashboardCtrl($scope, $http, $window, toastr) {

    }

    angular
        .module("boipeba.dashboard")
        .controller("dashboardCtrl", dashboardCtrl);

})();
